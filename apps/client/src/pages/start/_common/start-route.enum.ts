export enum StartRoutePaths {
  AUTH = 'auth'
}

export enum AuthRoutePaths {
  LOGIN = 'login',
  REGISTER = 'register',
  FORGOT_PASSWORD = 'forgot-password',
  PASSWORD_RESET = 'password-reset',
  VERIFY_EMAIL = 'verify-email'
}
